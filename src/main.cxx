#include "record.h"
#include "MyDataProcessThread.h"

#include <iostream>

int main() {
    // MyProcessThread dataProcess(0);
    // dataProcess.start();
    // dataProcess.join();

    //MyDataProcessThread dataProcess(0);
    //dataProcess.testRun();
    
    // Record record;
    // record.record_start();
}
